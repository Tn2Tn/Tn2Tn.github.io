            - name: Cache
  uses: actions/cache@v4.2.0
  with:
    # A list of files, directories, and wildcard patterns to cache and restore
    path:  path: |
            zzzzz
            images
            favicon
            90_Seconds/assets
            podcast/assets/lightbox
            podcast/assets/images/podcast 5
            podcast/assets/images/podcast 7
            podcast/assets/images/podcast 8
            podcast/assets/images/podcast 9
            podcast/assets/audio/Ahron Cowen.mp3
            podcast/assets/audio/Nosson_Perl_&_Ari_Gutwirth.mp3
            podcast/assets/audio/Eliezer Podcast.mp3
            podcast/assets/audio/Eli Netser.mp3
            podcast/assets/audio/Chaim Morgan.mp3
            podcast/assets/audio/Ari Hirsch & Gershy Bambeger.mp3
            podcast/assets/audio/Yehoshua Sayagh.mp3
            podcast/assets/audio/Story.mp3
            podcast/assets/audio/Stone_&_Jayson.mp3
            podcast/assets/audio/Sloimy Blun & surprise.mp3
            podcast/assets/audio/Shmuli Stimler & Lael Tam.mp3
            podcast/assets/audio/Purim24.mp3
            podcast/assets/audio/Problem.mp3
            
    # An explicit key for restoring and saving the cache
    key: ${{ runner.os }}-build-${{ hashFiles('**/package-lock.json') }}
    # An ordered multiline string listing the prefix-matched keys, that are used for restoring stale cache if no cache hit occurred for key. Note `cache-hit` returns false in this case.
    restore-keys: # optional
    # The chunk size used to split up large files during upload, in bytes
    upload-chunk-size: # optional
    # An optional boolean when enabled, allows windows runners to save or restore caches that can be restored or saved respectively on other platforms
    enableCrossOsArchive: # optional, default is false
    # Fail the workflow if cache entry is not found
    fail-on-cache-miss: # optional, default is false
    # Check if a cache entry exists for the given input(s) (key, restore-keys) without downloading the cache
    lookup-only: # optional, default is false
    # Run the post step to save the cache even if another step before fails
    save-always: # optional, default is false
          
